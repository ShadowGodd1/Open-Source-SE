apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: osee-kenya

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  # Secrets should be created separately for security reasons
  # - secrets.yaml
